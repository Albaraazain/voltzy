import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFD4B996);
  static const secondary = Color(0xFF8D7B68);
  static const accent = Color(0xFFA4907C);
  static const background = Color(0xFFF8ECD1);
  static const text = Color(0xFF1A120B);
  static const error = Color(0xFFB00020);
  static const success = Color(0xFF4CAF50);

  // Prevent instantiation
  AppColors._();
}
